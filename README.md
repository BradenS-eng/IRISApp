1. Clone the repo

2. Open the repo in VS Code
3. Set up an environment <\br>
python3 -m venv venv
venv\Scripts\activate.bat

5. Install dependencies
pip install -r requirements.txt

6. Execute the main Python script to open the software
python main.py
